using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.Net;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;


namespace Minsa.VPA.Repositorio
{
    public class RepositorioDeUtilidades
    {
        public static Context Contexto;
        //public static bool ExisteComunicacion()
        //{
        //    Android.Net.ConnectivityManager connectivityManager = (Android.Net.ConnectivityManager)Application.Context.GetSystemService(ConnectivityService);
        //    Android.Net.NetworkInfo activeConnection = connectivityManager.ActiveNetworkInfo;
        //    bool isOnline = (activeConnection != null) && activeConnection.IsConnected;
        //    if (isOnline == false)
        //    {
        //        return false;
        //    }
        //    else
        //    {
        //        return true;
        //    }
        //}

    }
}